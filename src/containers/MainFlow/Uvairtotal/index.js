import React from 'react';
import Totaluvair from './view';

const Uvairtotal = (props) => {

  const viewProps = {
    ...props,
  };

  return <Totaluvair {...viewProps} />;
};

export default Uvairtotal;